specVersion: "v1"
serviceId: "guard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "guard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
